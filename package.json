{
  "name": "fast_food",
  "version": "1.0.0",
  "description": "Web application for ordering food",
  "main": "index.js",
  "scripts": {
    "lint": "eslint ./routes --fix",
    "test": "nyc  --require babel-core/register mocha ./dist/test/appTest.js --exit",
    "coverage": "npm test && nyc report --reporter=text-lcov | coveralls",
    "build": "rm -rf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log",
    "start": "npm run build && nodemon dist/index.js"
  },
  "author": "Danielshow",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.3",
    "multer": "^1.3.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "chai": "^4.1.2",
    "chai-http": "^4.2.0",
    "coveralls": "^3.0.2",
    "eslint": "^5.5.0",
    "eslint-config-airbnb-base": "^13.1.0",
    "eslint-plugin-import": "^2.14.0",
    "istanbul": "^0.4.5",
    "mocha": "^5.2.0",
    "mocha-lcov-reporter": "^1.3.0",
    "nyc": "^13.0.1"
  }
}
